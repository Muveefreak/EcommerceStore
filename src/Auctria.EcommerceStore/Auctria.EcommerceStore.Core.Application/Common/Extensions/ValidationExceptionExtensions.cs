namespace Auctria.EcommerceStore.Core.Application.Common.Extensions;

public static class ValidationExceptionExtensions
{
    //public static IList<ValidationFailure> ValidationFailure(this IDictionary<string, string[]> input)
    //{
    //    return input
    //            .Where(x => x != null)
    //            .GroupBy(x => new { x.PropertyName, x.ErrorMessage })
    //            .Select(x => x.FirstOrDefault())
    //            .ToList();
    //}
}
